# Screenshots

Add these to make the repo pop on GitHub / LinkedIn:

1. `nodes-ready.png`   — `kubectl get nodes -o wide` showing both nodes Ready
2. `pods-running.png`  — `kubectl -n demo get pods -o wide` (pods spread on worker)
3. `https-padlock.png` — browser at https://k8s.chishty.me showing the 🔒 padlock
4. `cert-ready.png`    — `kubectl -n demo get certificate` showing READY=True

Then reference them in the main README (examples already stubbed there).
